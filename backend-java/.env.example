# Copy to .env and fill in. .env is git ignored and must never be committed.
# Spring reads these from the process environment, not from this file.
# Set them in your shell or in your IDE run configuration.
DB_HOST=
DB_PORT=5432
DB_NAME=
DB_SCHEMA=quoteownr
DB_USER=
DB_PASSWORD=
SERVER_PORT=8080
